Requirment : psutil, matplotlib

options:
* --dir-name : name of result folder. default is 'proc_info'
* --plot-only : if you only want to plot with csv, set this to True
* --csv-dir : name of csv-saved folder
